#include <iostream>
#include <stdio.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

struct alumno{
	int id;
	char nombre[50];
};

void nuevoAlumno(struct alumnos *alumnos, int *longitud){
	int i;
	for(i = 0; i< *longitud ; i++){
		alumnos++,
	}
}

int main() {
	struct alumno alumnos[10];
	int contadorAlumnos = 0;
	
	nuevoAlumno(alumnos, &contadorAlumno);
	
	
	
	return 0;
}
